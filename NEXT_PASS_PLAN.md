# 终末地主题：下一阶段精修规划

日期：2026-08-30

视觉基线：`0368a43 全系:校正灰底斜纹墨色`

本次菜单复修起点：`0fe4164 菜单:统一控件轮廓与交互状态`

## 一句话判断

主题的整体方向已经成立。下一阶段不再改大布局、换配色或继续堆纹理，而是通过统一
按钮、列表行、弹层和文字层级这些高频微观关系，继续消除“像终末地但显得廉价”的部分。

## 已锁定的基线

以下内容已经经过官方素材对照和普通 SillyTavern 运行时验证，后续默认不重新设计：

- 页面宽度：桌面保持用户默认的 50%，窄屏回到满宽。
- 表面体系：白色档案纸面 + `#e5e5e5` 灰色工作区 + 深色顶部控制条。
- 灰底结构：8px 暗灰斜十字细纹、160px 结构网格、断开套准点和局部淡白等高线。
- 主强调：纯黄只用于当前项、主操作、左侧识别轨和少数章节结构。
- 层次手法：靠面积、灰阶、边线和留白分层，不靠投影。
- 顶栏：当前分类继续使用整块黄色选中态；18 枚自绘描边功能图标已撤除，现用酒馆原生 Font Awesome 字形，不宣称官方图标复刻。
- 菜单：公共纸面外壳、直角字段和通用标题栏已经覆盖主要抽屉。
- 工程边界：继续走 `src/parts → build → themes/Endfield-Wuling.json`，不修改酒馆或插件源码。

## 当前剩余的廉价感来源

1. **控件之间缺少统一的字重和尺寸纪律。** 同一屏里的文字按钮、纯图标按钮、下拉箭头、
   折叠箭头仍像几个不同系统拼在一起。
2. **长列表过于机械重复。** 扩展、世界书和部分设置把每一行都画成同权重灰条，缺少
   “一级入口／当前项／展开内容／禁用项”的层级差。
3. **弹层体系还没有完成。** 下拉列表、右键菜单、确认弹窗、提示信息仍容易露出酒馆原生
   或通用网页组件感。
4. **信息密度尚未完全校准。** 小标签、说明文字、时间戳、字段标题的灰阶和间距不总是稳定，
   局部仍有“字都一样重要”的问题。
5. **动效虽有实录依据，但应排在静态精修之后。** 现在先把稳定帧做对，再复现 200–300ms
   的淡出／解析淡入；不新增扫光、故障闪烁或弹簧位移。

## 调整顺序

| 顺序 | 调整对象 | 预期收益 | 风险 | 证据与约束 |
| --- | --- | --- | --- | --- |
| P0（已完成） | 按钮与图标控件微系统 | 最高；几乎每个菜单都会受益 | 低 | 游戏设置实录、当前 API／扩展菜单 |
| P1（两个样板已部署，待目视确认） | 折叠行与连续列表 | 高；解决扩展页“灰条堆叠” | 中 | 地区建设折叠菜单实录 |
| P2 | 下拉、右键菜单、确认弹窗与通知 | 高；补齐跨页面一致性 | 中 | 设置下拉实录、官方白纸面组件 |
| P3 | 小字号信息层级与节奏 | 中；进一步去除模板感 | 低 | 官方公告长图和百科文档实录 |
| P4 | 过渡动画校准 | 中；增强终末地“解析感” | 中 | 三段游戏录屏的时间事实 |
| P5 | 手机真机、modern-ui、Luker | 发布前必须完成 | 高 | 分环境独立验收，不合并声明 |

## 已完成：按钮与图标控件微系统

这是上一轮完成的初步控件统一；下方的菜单减轮廓复修优先于本节旧形态描述。

### 范围

- 文字操作按钮：连接、附加参数、导入、保存、管理、安装等。
- 纯图标按钮：编辑、复制、删除、刷新、折叠箭头等。
- 状态：默认、悬停、键盘焦点、选中、禁用。
- 代表页面：API 菜单和扩展菜单；两者通过后再确认其他菜单是否自然继承。

### 设计规则

- 文字按钮与纯图标按钮必须是两类，不再共用同一个“白框胶囊”外形。
- 纯图标按钮保持方形、零圆角、统一笔画和视觉尺寸；不靠粗边或大底色抢视线。
- 文字按钮只在明确动作上使用轻胶囊或紧凑矩形；列表行和字段不能伪装成按钮。
- 当前／启用态使用深灰底或完整黄色块，依据原生语义选择一种，不叠加黄边、黄字和黄底。
- 禁用态只降低灰阶与对比，不额外增加纹理。
- hover、focus、active 只改变一个主要维度，避免同时变色、位移、发光和加粗。

### 验收

- API 顶部工具按钮与正文操作按钮能一眼区分用途。
- 扩展菜单两列中，折叠箭头不再比标题更抢眼。
- 桌面 1184px、页面宽 592px 时不换行错位；390px 时不横向溢出。
- 键盘焦点可见；禁用按钮仍可辨认但不会被误认为主操作。
- 不引入投影、玻璃质感、额外黄色装饰或新纹理。

### 本轮实测结论

- 黑色标题栏统一为 28px 高的直角工具轨：纯图标是 28×28，文字工具同高但按内容自适应宽度。
- 白色正文中的明确动作保留 28px 胶囊；形状由所在层级决定，不再由“有没有文字”偶然决定。
- 折叠箭头保留 22px 点击区，但取消常驻方框并把折线减到 1px，展开只靠方向表达。
- 默认、悬停、键盘焦点、选中和禁用状态均已命中真实元素；390、614、1184 三档无横向溢出。
- 运行时证据见 `research/runtime/p0-controls-*-before.png` 与 `p0-controls-*-after-*.png`。

## 已完成：菜单减轮廓与标题层级复修（2026-08-30）

- 只改菜单内部，未改欢迎页、正文阅读区、灰底纹理和页面宽度。
- 真正的抽屉顶栏保留一次 `//` 标题；普通 API 标签由 17.55px/700 降到 13.5px/650。
- 普通 h4 采用正文体 13.5px/650；用户设置分区采用 700 字重、2×14px 黄短标和单条底线，取消渐变底、粗黑轨和重复 `//`。
- 字段静止态只保留浅填充和一条底线；按钮保留填充但移除常驻完整边框；黑色顶栏工具使用无框白字形。
- 折叠内容去掉三边套框；用户设置展开前后左坐标与内边距不变。
- 修正 Persona 旧黑栏选择器误包整个正文的问题，黑栏只命中真正标题行。
- 通用悬停排除选中/启用/禁用态；黑栏用控件变量供色，避免高优先级覆盖状态。
- 390、614、1184 三档九个菜单共 27 次边界检查，抽屉横向溢出均为 0，标题/字段/按钮均未越出抽屉。
- 格式化菜单真实启用按钮悬停后仍为深灰底+黄字；文本字段焦点为深灰细轮廓+黄短标。
- 证据：`research/runtime/outline-density-qa-2026-08-30.md` 与同目录 `outline-density-*` 截图。

## 已完成：撤除自绘功能图标（2026-08-30）

- 18 枚 `--ef-icon-*` 是项目本地简化线稿，不是官方图标或成熟图标库原件。
- 已整批撤除顶栏/输入栏/用户设置操作的蒙版覆盖，恢复 DOM 已有的 Font Awesome 字形，不引入第二个图标库。
- 只校准伪元素字号：顶栏 17px、输入栏 18px、设置操作 15px；父字号和点击区域未改。
- 18 枚 × 390/614/1184 三档，共 54 项检查：字形均非空、蒙版均为 none、点击区宽高变化均为 0。
- 顶栏 37.5×32px；输入工具 34.5×34.5px、发送 46×34.5px；设置预设操作 28×28px、账户 73.35×30px，均与替换前一致。
- 发送恢复为纸飞机；设置、扩展、导入/导出恢复原生功能语义。纹理和背景 SVG 未动。
- 主题由 94566 字节减至 86338 字节；除 custom_css 外的 JSON 字段没有变化。
- 证据：`research/runtime/icons-qa-2026-08-30.md` 与 `icons-*-614.png`、`icons-after-390.png`、`icons-after-1184.png`。

## 后续各刀的边界

### P1 样板：下载资源／角色表情（2026-08-30）

- 本轮只命中 `#assets_ui`、`#expressions_container > .expression_settings` 的第一个原生折叠模块，未推广到其余扩展。
- 重新对照用户录屏的地区建设折叠关键帧，借用一级入口与内容面的层级关系；不复制无对应语义的黄色图标块，不把整个设置改深色。
- 一级入口为纸白面、14px/650；实际行高从 30.8px 增至 34.8px，箭头仍为 22×22px。
- 展开内容使用极浅灰面和 10px 横内距；选项/字段为 13.5px，字段标签为 13px/600，说明为 12px/400、`#626967`。
- 组间 12px；说明的上下间隔为 2px/6px，取消原生 small 的重复透明度。空状态提示不再作为居中粗标题。
- 不修改功能开关、API 选项、库数据或插件代码；原生条件显隐仍由酒馆控制。上下内距不使用 `!important`，保留原生折叠动画插值。
- 390/614/1184 三档展开与收起终态无横溢；1184px 下仍为 592px 面板、约 280.05px 两列。键盘焦点与 Enter 展开通过。
- 样板没有真实“禁用一级入口/当前选中行”语义，未伪造状态，也不声明这两种状态已验收；图库、分类器切换后的内容、真实手机尚未验收。
- 证据：`research/runtime/p1-extensions-qa-2026-08-30.md` 和 `p1-extensions-*` 对比截图。

### P1：折叠行与连续列表

- 先处理扩展菜单，再复用到世界书等长列表。
- 一级标题、当前项、展开内容和禁用项必须有不同权重。
- 展开依靠正常高度变化推动后续内容，不做悬浮、缩放或弹跳。
- 保留两列扩展布局和原生点击区域，不重排功能结构。

### P2：弹层与短生命周期界面

- 建立统一的下拉列表、上下文菜单、确认弹窗和通知表面。
- 白色列表的当前项可用深灰行 + 细标记；危险操作只在语义确实危险时出现警示色。
- 先覆盖真实出现的组件，不凭选择器名称批量猜测。

### P3：文字与间距精修

- 校准 `h3/h4`、字段标题、说明文字、时间戳和数字标签的灰阶、字号与行距。
- 优先减少同权重粗体；不再创造新的英文装饰标签。
- 正文阅读区保持大负空间，不因“更像 UI”而压缩文本。

### P4：动效

- 只复现已确认的短淡出、空白解析间隔和淡入，目标时长 200–300ms。
- 折叠项只做高度变化与箭头同步；不增加弹簧或横向滑动。
- 全部保留 `prefers-reduced-motion` 与酒馆减少动画路径。
- 如果必须根据动态数量错峰或监听聊天事件，再触发可选薄插件评估；当前不需要拆包。

## 每一刀的固定工作流

1. 只选一个代表组件和一个对照组件。
2. 修改对应源码分片，不直接手改构建 JSON。
3. `npm run check` 与 `git diff --check`。
4. 部署至普通 SillyTavern，确认源文件与部署文件哈希一致。
5. 在 1184×800（50% 页面宽度）、614×695、390×844 三档检查布局与截图。
6. 关闭测试抽屉，按明确文件清单提交一条可回滚 commit。
7. 用户目视通过后再进入下一刀。

## 暂时不要做

- 不重新调整已定稿的灰底颜色、斜纹、套准点和等高线。
- 不把设置整体改成深色；用户已明确这不是当前必要项。
- 不继续加入版本主题、地区配色或宣传大标题。
- 不全量拆包；只有现有图标／纹理无法解决明确差异时才做定向 UI 资源取证。
- 不同时修改按钮、列表、弹窗和正文排版。

## 续作入口

**P1 的下载资源／角色表情样板已部署，先等用户目视确认。** 其余扩展保留旧样式作为对照，不自动推广。
若方向通过，再检查快速回复等异构结构以及真实存在的禁用/选中状态，逐类复用，而非一次覆盖所有后代。
后续不要重新堆回粗左轨、完整卡片边界或装饰性编号；不要顺手修改弹窗、正文或已锁定的灰底。
