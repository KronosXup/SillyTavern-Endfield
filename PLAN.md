# 终末地 SillyTavern 主题项目计划

## 当前状态

- 状态：**β0.3「档案长图」方向在推进**。α1 的武陵暗青视觉前提已作废，只留作工程链路样本。
- 证据源已换代：主证据是**官号发布的白底长图附件**（TapTap／微博／B 站动态，四组，
  见 `research/reference/official-longform/README.md`）。拆包贴图降级为局部形态参考，
  不再作为整套主题的视觉依据 —— 单张装饰贴图推不出界面骨架。
- 证据优先级已锁定：锦陇／四号谷地／帝江号的中性档案长图是唯一母版组；
  `official-system-longform` 中的协议通行证、版本内容、功能前瞻和当期档案只作组件补充。
  “发布时间更近”不能覆盖“跨版本可统一”，任何版本配色、角色主视觉和地区皮肤都不得反推全局主题。
- 已确认游戏资源：`<游戏安装目录>/Endfield_Data/StreamingAssets/VFS`，约 57 GB 的 `.chk` 加少量 `.blc` 索引。
- 已确认测试宿主：普通 SillyTavern 1.18.0 与 Luker 2.7.0；首要验收仍以普通 SillyTavern 为准。
- 工作区：已建立 `src → build → themes` 维护边界并初始化 Git；设计基准与参考素材分批提交，便于回滚。
- 首要交付：可直接导入普通 SillyTavern 的单文件主题 `themes/Endfield-Wuling.json`
  （文件名保留旧后缀，内容已非武陵）。
- 可选交付：仅在 CSS 无法承担关键动效时，增加一个薄型动画插件。
- 已定主视觉：**官号档案长图的连续纸面阅读区**。白／极浅灰纸面、左侧窄荧光黄识别轨、
  两位数编号分节、局部深灰媒体块；黄色只承担左轨、章节条、关键词标记与主操作。

## 项目原则

1. **从终末地本身取证**：颜色、字体、几何、图标和动效不凭印象猜，也不套用现有 Nord、Gruvbox 或明日方舟主题骨架。
2. **定向拆包**：先建立组件清单，再查找对应资源；不进行无目的的全量导出。
3. **动效是一等设计材料**：静态资源与 60fps 动态采样同步进行，建立可追溯的动效事实表。
4. **JSON-first**：视觉样式、CSS 微动效和素材 data URI 进入主题 JSON；不修改 SillyTavern、Luker 或第三方插件源码。
5. **一刀一刀改**：每次只完成一个组件或一类状态，构建、验证、试用后再继续。
6. **兼容顺序固定**：普通 SillyTavern 桌面端 → 普通 SillyTavern 手机端 → modern-ui → Luker。
7. **诚实标记验证级别**：区分“研究候选”“离线验证”“普通 ST 实机通过”“其他宿主通过”。
8. **全局与地区分层**：主题先建立终末地通用协议骨架；武陵、四号谷地等地区语言只在证据支持的组件与状态中出现，不做五五开拼贴，也不让单个地区统治整套界面。
9. **版本皮肤不混合**：版本主题首先服务当期角色、地区与活动，彼此难以统一；只能取与中性档案母版一致的局部结构，不能混合成所谓“终末地综合风格”。

## 最终交付边界

### 单 JSON 必须承担

- 颜色、字体、间距、边线、切角和层级令牌。
- 顶栏、聊天区、消息、输入区、抽屉、弹窗和常用表单的视觉覆盖。
- 现有图标的 CSS mask／data URI 替换。
- hover、focus、active、开合和单元素入场等 CSS 动效。
- 桌面与手机响应式布局。
- Reduced Motion 降级。

### 插件升级闸门

出现以下任一“必须实现”的需求时，才评估可选插件：

- 需要监听 `APP_READY`、`CHAT_CHANGED` 或消息事件主动播放动画。
- 多个组件必须按严格时序编排。
- 错峰延迟必须根据动态项目数量计算。
- 需要新增 DOM 组件、运行时开关或持久化主题设置。
- CSS 原型无法复现已选定的标志性动效。

插件只负责事件触发与时序，不重复承载主题 CSS。

## 阶段与验收闸门

### 阶段 0：范围与环境基线

目标：先确认我们到底为哪个宿主、哪类用户和哪种发布方式制作。

任务：

- 确认主要测试端、版本、用户目录和主题部署目录。
- 区分普通 ST、modern-ui 与 Luker，不混用 DOM 假设。
- 确认终末地游戏安装位置和可读取的资源范围。
- 确认成品是私人高还原版、公开发布版，还是两者分流。
- 明确首版只做暗色、亮色，或单一官方主界面方向。

产物：宿主矩阵、资源来源说明、发布边界。

闸门：一句话目标、首要宿主和首版视觉方向都已明确。

### 阶段 1：界面与动效取证

目标：建立可以设计和复现的事实，而不是堆截图。

静态采样：

- 武陵地区总览、协议核心、地区建设／事务总览、流体 AIC 工业、任务提示，以及通用列表、弹窗和设置状态。
- 四号谷地只采集同构界面作为 A/B 对照，用于分离“终末地全局语言”和“武陵地区语言”。
- 记录分辨率、界面状态、组件用途和截图来源。
- 建立顶栏、标签、按钮、卡片、输入框、图标、分隔线和装饰轨道组件表。

动态采样：

- 以 60fps 优先录制武陵相关页面开合、地图／总览切换、流体或管线状态、按钮反馈、列表进入和完成反馈。
- 逐帧记录触发条件、时长、延迟、位移、透明度、缩放、裁切、缓动和退出方式。
- 区分高频系统动作与只出现一次的演出动作。
- 不把“流体”简化成到处漂浮的蓝色波纹；只有实机证据支持时，才转译为流向、连通、填充或沿轨道传播的动效。

在线参考分级：

- **事实源**：本机当前游戏版本的实机截图、60fps 录屏和定向提取资源。
- **线索源**：公开的资源路径表、游戏脚本常量、官方版本说明和带时间戳的实机视频。
- **分析源**：现成交互拆解只负责提出假设与反例，例如模块网格、非对称平衡、克制配色和交互可见性问题。
- **实现参考**：同人组件库和伴侣应用可用于研究 CSS 与动效组织，但不得反向当成终末地设计事实。

产物：截图索引、组件事实表、动效事实表。

闸门：核心组件和关键动效均有来源；不再凭感觉指定色值或动画时长。

### 阶段 2：定向资源勘探与拆包

目标：获取终末地真实的 UI 设计系统，避免全量拆包失控。

优先查找：

- 名称或路径包含 `wuling`、`xiran`、`fluid`、`aic`、`region`、`map` 的 UI 资源与配置。
- UI 图集、九宫格面板与边框。
- Logo、企业标记、区域标识和警告符号。
- 功能图标及普通／选中／禁用状态。
- 字体文件、字体图集或可识别的字形配置。
- 小型底纹、噪点、遮罩、渐变和扫描材质。
- 与 UI 过渡有关的动画片段、控制器、时间线或配置元数据。

明确排除：

- 角色模型、立绘、剧情 CG、场景大贴图、战斗特效、音频和其他非 UI 资源。

处理规则：

- 保留原始文件名、路径、尺寸和哈希。
- `raw`、`selected`、`derived` 三层分离；原始资源不直接混进成品。
- 优先把结构重画成 CSS；只有不可替代的小型素材才进入 JSON。

现成工具路线（2026-08-20 初筛）：

1. 首选 `EIHRTeam/EndfieldStudio`：源码可审计，支持 `.blc/.chk`、UnityFS、Texture2D/Sprite 解码，以及按资源名／包名正则过滤；先从源码构建并做 UI 小样本。
2. `RoyZ-iwnl/Beyond-VFS-Decrypt` 作为较小的 Python 协议参考和故障排查后备，不作为主要批量工作流。
3. `Variante/endfield_research_kit` 仅在后续需要跨资源检索时启用 focused/lean 路线；不执行默认全量资产导出。
4. `Nemra520/Beyond_BLCVFS` 因特殊删除声明、非商业相同方式共享许可和引用来源复杂，暂不作为首选执行工具。
5. 任何第三方工具先固定源码版本、审查依赖和输出路径；原始提取物不得进入主题成品或公开仓库。

产物：资源地图、精选素材清单、资源与截图的对应关系。

闸门：已经能回答每个核心组件“来自哪里、如何转译、是否需要嵌入”。

### 阶段 3：设计与动效系统

目标：把游戏 UI 事实转化为适合文字聊天的设计语言。

任务：

- 从证据中提取 4–6 个核心颜色和语义色。
- 定义标题、正文、数据／编号至少三类字体角色。
- 定义切角、边线、网格、留白、密度和层级规则。
- 保留终末地黑／白／灰协议骨架，但不让四号谷地式警戒黄和硬边机械感统治整个主题。
- 将武陵“流体、连通、转化”的地区机制转译为信息结构：状态沿轨道流动、关联项明确连线、阶段变化表现为受控填充；具体颜色与曲率等待截图和资源采样后确定。
- 选择一个高频、辨识度强的动作作为标志性过渡；其余动效保持克制。
- 为聊天区、消息、输入区和抽屉制作桌面／手机线框。
- 做一次“泛科幻自检”：删掉不能由终末地证据解释的装饰。

产物：视觉令牌、动效令牌、线框和设计命题。

闸门：用户认可“这是终末地，不是普通黑黄工业风”。

### 阶段 4：单 JSON 工程骨架

目标：建立可维护的源码，而不是直接编辑巨大 JSON 字符串。

当前结构：

```text
src/
  base-theme.json
  parts/
    01-tokens.css
    02-foundation.css
    03-chat.css
    04-input.css
    05-motion.css
references/
scripts/
  build-theme.mjs
  verify-theme.mjs
  extract-ui-samples.ps1
themes/
  Endfield-Wuling.json
```

构建要求：

- 将分件 CSS 和精选 data URI 注入 `custom_css`。
- 校验 JSON 可解析、CSS 非空、素材引用完整。
- 默认不依赖外部 URL 或远程字体。
- 构建不修改主题 JSON 的无关原生字段。

产物：可重复构建的 α1 主题；5 个 CSS 分片，构建后 JSON 为 6,679 字节。

闸门：已通过。普通 ST 1.18.0 可识别、切换并在重载后保持主题；无远程依赖，运行页控制台无错误。

### 阶段 5：第一刀可玩原型

目标：用最小范围验证整个方向。

α1 已实现：

- 聊天背景的基础层级。
- 用户／角色消息共用的武陵流线侧轨与分层底色。
- 输入区与发送按钮。
- 一个标志性过渡及 Reduced Motion 降级。

暂不实现顶栏、抽屉、设置、世界书、modern-ui 和 Luker。

产物：`Endfield-Wuling.json` 的第一份可导入候选。

闸门：技术验收已通过；等待用户目视确认视觉和动效方向后再扩展顶栏。

### 阶段 6：核心聊天闭环

按以下顺序逐刀完成：

1. 消息正文、引用、代码块和媒体。
2. 生成中、编辑、滑动、删除和消息操作按钮。
3. 通知、弹窗和菜单。
4. 左右抽屉、角色列表和常用表单。
5. 设置、世界书及其他低频界面。

每刀都执行构建、JSON 校验、选择器命中、尺寸探针和用户目视验证。

闸门：普通 ST 桌面端核心流程无重叠、裁切和不可操作控件。

### 阶段 7：手机与扩展环境

顺序：

1. 375–430px 视口布局探针。
2. 手机／平板真机操作。
3. `body.th-modern-enabled` 限定的 modern-ui 适配。
4. Luker 特有界面适配。

产物：兼容矩阵和未覆盖清单。

闸门：每个环境分别标记“未测”“离线通过”或“实机通过”，不合并声称兼容。

### 阶段 8：插件决策与发布

- 回看标志性动效是否被 CSS 正确承载。
- 只有触发插件闸门时才制作可选动画插件。
- 检查 JSON 体积、CSS 解析、动画合成和素材许可。
- 私人高还原版与公开发布版如有素材边界，分别构建。
- 完成安装说明、切主题重载提示和已知限制。

闸门：普通 ST 真实导入和新聊天验收完成后，才称为可发布版本。

## 下一项实际工作

β0.3 已落地的装置（均经普通 ST 桌面端与 390px 探针验证，见 `references/VALIDATION.md`）：

- 纸面 + 约 10px 微网格 + 约 160px 交点定位十字。
- 左侧全高荧光黄识别轨。
- 扁平消息行 + 细分隔线，零投影（深度只来自面积对比与线）。
- 顶栏图标全替换为硬边描边字形。
- 楼层编号（`09-floor-index.css`）：取 ST 原生 `mesid`，个位补零成两位。
- 分节横条（`10-section-band.css`）：黑色编号块 + 黄色横条 + 粗体标题 + 极小英文标签，
  编号用 CSS counter 自动排，只挂真实存在的分节标题。
- 设置面板整体改造（`07-user-settings.css`）。

按顺序继续：

1. **深灰收束区**。官方长图每章结尾／危险内容用深灰块收束，正文里目前完全没有。
2. **顶栏与抽屉**。顶栏图标已换，容器与展开态尚未按档案语言处理。
3. **弹窗与菜单**。
4. **手机端逐组件目视**（现在 `scripts/shot.mjs --probe --width 390` 可直接按视口探针）。

### 已明确不做的事

- **不给每条消息套完整章节头**。聊天区没有原生分节边界，日期条是卡片正则产物
  （`.mes_text` 里的 `<details class="sp-...">`），挂上去会把主题绑死在某张卡上。
  逐条消息只用楼层编号，章节装置留给真实标题。
- **不在主题层修消息内容缺陷**。正文首段的 `<p><br><br></p>` 空带来自卡片模板／正则，
  CSS 无法安全消除（`:empty` 不匹配、结构选择器会误杀真实内容），应在卡片正则层修。
- 动效仍按 `references/MOTION_FACTS.md` 闸门冻结：没有带时间戳的逐帧录屏事实表前，
  不新增扫描线、故障闪烁、粒子或大幅页面转场。
