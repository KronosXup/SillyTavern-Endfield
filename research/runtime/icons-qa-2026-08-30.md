# 原生功能图标恢复回归记录

日期：2026-08-30。宿主：普通 SillyTavern 1.18.0，`http://127.0.0.1:8000/`。
起点：`357b698`，菜单减轮廓版本。

## 改动范围

- 删除 `01-tokens.css` 中 18 枚项目自绘图标变量。
- 删除 `06-controls.css` 中顶栏/输入栏的映射与蒙版，以及 `07-user-settings.css` 中六枚操作图标的映射与蒙版。
- 恢复 DOM 已有的 Font Awesome 6 Free 字形。没有新增图标库、下载字体或声称使用官方游戏图标。
- 仅设置图标伪元素字号、行高与垂直对齐：顶栏 17px，输入栏 18px，设置操作 15px。
- 父元素字号、按钮宽高、菜单结构、标题和配色未改；背景纹理 SVG 未改。

## 运行时结果

18 个目标在 390×844、614×695、1184×800 下分别与替换前比较，共 54 项：

- 所有目标 `::before.content` 非空。
- 所有目标 `mask-image` 为 `none`。
- 所有目标的字体为 `Font Awesome 6 Free`，字体加载检查通过。
- 所有点击区的宽度/高度差值为 0。

| 区域 | 点击区实测 | 替换后字形 |
| --- | --- | --- |
| 顶栏九枚 | 37.5×32px | 17px |
| 输入栏两枚工具 | 34.5×34.5px | 18px |
| 发送 | 46×34.5px | 18px，原生纸飞机 |
| 设置五枚预设操作 | 28×28px | 15px |
| 账户按钮 | 73.35×30px | 15px，父图标占位未缩 |

已目视检查 614px 设置页、614/390/1184 首页顶栏与输入栏，没有发现字形裁切。
最终恢复到 614×695，测试抽屉全部关闭，主题仍为 `Endfield · 档案长图 β0.3`。

## 构建与边界

- `npm run check`、`git diff --check` 通过。
- JSON 除 `custom_css` 外字段变化为空；产物 86338 字节，比上一刀减少 8228 字节。
- 构建文件与部署文件 SHA-256 一致：`3176A994233703EDA591F65CC32F064B25E78C39DF0752D638D6D0F3BE37026C`。
- 未点击发送、导入、导出、删除或保存操作；没有通过测试制造数据副作用。
- 这是普通酒馆浏览器视口验证，不等同于手机真机、modern-ui 或 Luker 验收。

## 截图

- `icons-before-614.png` / `icons-after-614.png`
- `icons-settings-before-614.png` / `icons-settings-after-614.png`
- `icons-after-390.png`
- `icons-after-1184.png`
