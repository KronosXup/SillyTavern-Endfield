# P1 原生快速回复层级验收

日期：2026-08-30。修改前：`64cb512`。

## 范围

- 普通 SillyTavern 1.18.0，`http://127.0.0.1:8000/`，原生快速回复设置 `#qr--settings`。
- 复用已部署的P1入口/浅灰内容面；只调整设置内部小节、集合行、工具区和回复编辑行。
- 用户实录 `research/video-reference/2026-08-27/keyframes/accordion-expanded.jpg` 用作一级入口/内部内容区分的参考，不复制游戏功能或无语义标记。
- 不修改酒馆或扩展源码、功能设置、回复内容、排序和集合数据。
- 明确排除第三方QR助手、`#qr--bar`、`#qr--popout`、上下文菜单及弹窗编辑器。

## 问题与调整

原生结构只有一个外层折叠；内部四小节为普通标题和内容，不是嵌套折叠。
原来的工具排布在50%宽度下挤压标题、集合名与编辑字段。

| 项目（1184px视口） | 修改前 | 修改后 |
| --- | --- | --- |
| 中央面板 | 592px | 592px |
| 两列宽度 | 280.05px + 280.05px | 不变 |
| 编辑小节标题 | 挤为三行 | 一行，13px/600 |
| 集合行选择框 | 49.19px | 220.05px |
| 编辑器集合选择框 | 与六工具挤在一行 | 260.05px独立一行 |
| 回复标签输入 | 24.26px | 183.80px |
| 回复正文输入 | 85.47px × 44.4px | 260.05px × 54px |
| 每条回复的五工具 | 竖向堆叠 | 同一行，146px总宽 |

- 一级入口沿用14px/650、纸白面；内部小节13px/600，说明与空状态12px。
- 集合行两排网格：拖柄保持独立，名称一排、显示和操作一排。
- 回复行三排网格：拖柄/标签/更多、正文、工具；原生子节点与排序单元不变。
- 没有新增粗轨、编号、纹理、投影或章节装饰。
- 未勾选“Buttons”只是隐藏聊天栏按钮，不被误画为整行禁用。

## 运行时检查

| 视口 | 面板宽 | 列宽与结构 | 面板/模块横溢 | 标题/字段越界 |
| --- | --- | --- | --- | --- |
| 390×844 | 390px | 364.70px，原生纵向排列 | 0 / 0 | 无 |
| 614×695 | 614px | 588.70px，原生纵向排列 | 0 / 0 | 无 |
| 1184×800 | 592px | 280.05px两列 | 0 / 0 | 无 |

- 当前42个操作按钮 × 三档＝126项尺寸对比，宽高变化均为0；更多按钮仍28.25×28px，常规工具仍26×26px。
- 修改前后每档8个复选框的checked/disabled状态完全一致；没有为了样式测试切换设置。
- 三条原生回复条目的工具行均同高排列；条目顺序仍为0、1、2。
- 收起稳定终态为display:none、高度0；Tab/Enter后可展开，深灰键盘焦点可见。
- `#qr--set-importFile`仍为display:none；三个插入条静止opacity均为0，悬停显现路径保留。
- 集合移除按钮悬停实测：文字色`rgb(142,51,39)`不变，填充从`rgba(142,51,39,.075)`变为`.15`。
- 危险与复制成功样式保留原生状态类，并排除disabled；未制造不存在的selected/disabled行状态。

## 验证边界

- 未点击增删、导入导出、剪贴板操作、执行回复或拖动排序。这些原生操作可能立即保存，因此只做布局/状态检查。
- 未实际触发复制成功回调；只通过源代码与CSS优先级审查其反馈恢复。
- 当前只显示已有Default集合与三条原生示例，不将其冒充超长自定义集合或复杂脚本压力测试。
- 桌面浏览器窄视口不等于手机真机；modern-ui、Luker与第三方改造未验收。

## 文件与证据

- 源码仅改`src/parts/14-menu-system.css`，构建为`themes/Endfield-Wuling.json`；除custom_css外的主题字段差异为0。
- `npm run check`、`git diff --check`通过。
- 源/部署SHA256：`26B2217208D3F352172C0D86120E3389203DBE36F0DC9C833D29817BF76539C8`。
- `p1-quick-reply-before-{390,614,1184}.png` / `p1-quick-reply-after-{390,614,1184}.png`：三档上部小节对比。
- 390px修改后截图保留了键盘测试焦点；主入口的细框是focus-visible，不是默认常驻轮廓。
- `p1-quick-reply-editor-before-1184.png` / `p1-quick-reply-editor-after-1184.png`：最窄桌面列的回复编辑区对比。
